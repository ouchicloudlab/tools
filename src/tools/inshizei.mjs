export default {
  category: "money",
  updated: "2026-08-27",
  title: "印紙税額の計算｜契約書・領収書に必要な収入印紙がわかる",
  h1: "収入印紙の金額 早見ツール",
  description:
    "契約書や領収書の金額を入れると、必要な収入印紙の額を表示します。文書の種類ごとの税額表つき。印紙が不要になる条件も確認できる無料ツールです。",
  cardText: "契約書・領収書に必要な印紙税額を判定。",
  keywords: [
    "印紙税", "収入印紙", "計算", "領収書", "契約書", "金額", "いくら", "非課税", "電子契約",
  ],
  yomi: "いんしぜい しゅうにゅういんし",
  related: ["kansuji", "shohizei-keisan"],

  ui: `
<div class="field">
  <label for="doc">文書の種類</label>
  <select id="doc">
    <option value="receipt" selected>領収書・レシート（第17号文書）</option>
    <option value="contract2">工事請負契約書（第2号文書）</option>
    <option value="contract1">不動産の売買契約書（第1号文書）</option>
    <option value="loan">金銭消費貸借契約書（第1号文書）</option>
  </select>
</div>

<div class="row">
  <div class="field">
    <label for="amount">記載金額（円）</label>
    <input type="number" id="amount" inputmode="decimal" value="55000" step="1000">
  </div>
  <div class="field">
    <span class="field-label">入力した金額は</span>
    <div class="pills" id="taxIncluded">
      <label><input type="radio" name="taxIncluded" value="in" checked>税込</label>
      <label><input type="radio" name="taxIncluded" value="ex">税抜（本体）</label>
    </div>
  </div>
  <div class="field">
    <label for="rate">消費税率（%）</label>
    <input type="number" id="rate" inputmode="decimal" value="10" step="1">
  </div>
</div>

<div class="field">
  <div class="pills">
    <label><input type="checkbox" id="separated" checked>領収書に消費税額を分けて記載する</label>
  </div>
  <p class="hint">消費税額を明記すれば、税抜金額で判定できます。</p>
</div>

<div class="result" aria-live="polite">
  <div class="result-label">必要な収入印紙</div>
  <div class="result-main" id="stampVal">-</div>
  <div class="result-grid">
    <div><div class="k">判定に使う金額</div><div class="v" id="judgeVal">-</div></div>
    <div><div class="k">消費税額</div><div class="v" id="taxVal">-</div></div>
    <div><div class="k">次の段階まで</div><div class="v" id="nextVal">-</div></div>
    <div><div class="k">文書の区分</div><div class="v" id="docVal">-</div></div>
  </div>
  <p class="result-sub" id="detail"></p>
</div>

<h3>税額表</h3>
<div class="table-wrap">
  <table>
    <thead><tr><th>記載金額</th><th>印紙税額</th></tr></thead>
    <tbody id="tableBody"></tbody>
  </table>
</div>
`,

  script: `
(function () {
  // [その段階の上限金額（この額を含む）, 税額]
  // 印紙税法の区分は「100万円以下」のように上限を含むため、
  // 判定は「金額 <= 上限」で行う。非課税の行だけ上限を1円下げてある。
  var RECEIPT = [
    [49999, 0], [1000000, 200], [2000000, 400], [3000000, 600],
    [5000000, 1000], [10000000, 2000], [20000000, 4000], [30000000, 6000],
    [50000000, 10000], [100000000, 20000], [200000000, 40000],
    [300000000, 60000], [500000000, 100000], [1000000000, 150000],
    [Infinity, 200000]
  ];
  // 建設工事の請負に関する契約書（軽減措置の適用後）
  var CONTRACT2 = [
    [9999, 0], [1000000, 200], [2000000, 200], [3000000, 500],
    [5000000, 1000], [10000000, 5000], [50000000, 10000],
    [100000000, 30000], [500000000, 60000], [1000000000, 160000],
    [5000000000, 320000], [Infinity, 480000]
  ];
  // 不動産の譲渡に関する契約書（軽減措置の適用後）
  var CONTRACT1 = [
    [9999, 0], [500000, 200], [1000000, 500], [5000000, 1000],
    [10000000, 5000], [50000000, 10000], [100000000, 30000],
    [500000000, 60000], [1000000000, 160000], [5000000000, 320000],
    [Infinity, 480000]
  ];
  // 金銭消費貸借契約書（軽減措置なし）
  var LOAN = [
    [9999, 0], [100000, 200], [500000, 400], [1000000, 1000],
    [5000000, 2000], [10000000, 10000], [50000000, 20000],
    [100000000, 60000], [500000000, 100000], [1000000000, 200000],
    [5000000000, 400000], [Infinity, 600000]
  ];

  var DOCS = {
    receipt: { table: RECEIPT, name: "第17号文書（金銭の受取書）", free: 50000,
      note: "記載金額5万円未満は非課税です。" },
    contract2: { table: CONTRACT2, name: "第2号文書（請負に関する契約書）", free: 10000,
      note: "建設工事の請負契約は軽減措置が適用された額です。" },
    contract1: { table: CONTRACT1, name: "第1号文書（不動産の譲渡）", free: 10000,
      note: "不動産の譲渡契約は軽減措置が適用された額です。" },
    loan: { table: LOAN, name: "第1号文書（金銭消費貸借）", free: 10000,
      note: "借用書やローン契約書が該当します。軽減措置はありません。" }
  };

  function lookup(table, amount) {
    for (var i = 0; i < table.length; i++) {
      if (amount <= table[i][0]) return table[i][1];
    }
    return table[table.length - 1][1];
  }
  // いまの段階の上限まであといくらか（超えると次の税額に上がる）
  function nextStep(table, amount) {
    for (var i = 0; i < table.length; i++) {
      if (amount <= table[i][0]) {
        return table[i][0] === Infinity ? null : table[i][0] - amount;
      }
    }
    return null;
  }

  ST.live(function () {
    var doc = DOCS[ST.$("doc").value];
    var input = Math.max(0, ST.n(ST.$("amount")));
    var rate = Math.max(0, ST.n(ST.$("rate")));
    var isIn = ST.pick("taxIncluded") === "in";
    var sep = ST.$("separated").checked;

    // 税抜と税込を求める
    var ex = isIn ? input / (1 + rate / 100) : input;
    var tax = isIn ? input - ex : input * rate / 100;
    ex = Math.round(ex);
    tax = Math.round(tax);
    var inc = ex + tax;

    // 消費税額を区分記載していれば税抜で判定できる
    var judgeAmount = sep ? ex : inc;
    var stamp = lookup(doc.table, judgeAmount);
    var next = nextStep(doc.table, judgeAmount);

    ST.set("stampVal", stamp === 0 ? "不要（非課税）" : ST.yen(stamp, 0));
    ST.set("judgeVal", ST.yen(judgeAmount, 0) + (sep ? "（税抜）" : "（税込）"));
    ST.set("taxVal", ST.yen(tax, 0));
    ST.set("nextVal", next === null ? "-" : "あと " + ST.yen(next, 0) + " で上がる");
    ST.set("docVal", doc.name);
    ST.set("detail", doc.note +
      (sep
        ? "消費税額を分けて記載しているため、税抜 " + ST.yen(judgeAmount, 0) + " で判定しています。"
        : "消費税額の記載がないため、税込 " + ST.yen(judgeAmount, 0) + " 全体で判定しています。") +
      (judgeAmount >= doc.free && !sep && tax > 0 && lookup(doc.table, ex) < stamp
        ? "※ 消費税額を分けて書けば " + ST.yen(lookup(doc.table, ex), 0) + " に下がります。"
        : ""));

    // 税額表。各行は「前の段の上限＋1 〜 その段の上限」を表す。
    var prev = -1;
    var marked = false;
    ST.$("tableBody").innerHTML = doc.table.map(function (r) {
      var label = r[0] === Infinity
        ? ST.yen(prev, 0) + " を超えるもの"
        : (prev < 0 ? ST.yen(r[0], 0) + " 以下"
          : ST.yen(prev + 1, 0) + " 〜 " + ST.yen(r[0], 0));
      var hit = !marked && judgeAmount <= r[0];
      if (hit) marked = true;
      prev = r[0];
      return "<tr" + (hit ? ' style="font-weight:700"' : "") + "><td>" + label +
        "</td><td>" + (r[1] === 0 ? "非課税" : ST.yen(r[1], 0)) + "</td></tr>";
    }).join("");
  });
})();
`,

  intro: `
契約書や領収書の金額を入れると、必要な収入印紙の額が分かります。**消費税額を分けて書くかどうかで印紙代が変わる**ケースも判定します。
`,

  guide: `
## 印紙税とは

契約書や領収書など、特定の文書を作成したときにかかる税金です。文書に収入印紙を貼り、消印（割印）を押すことで納付します。

課税されるのは印紙税法で定められた **20種類の文書** に限られます。よく使われるのは次の3つです。

| 号数 | 文書の種類 | 非課税の範囲 |
|---|---|---|
| 第1号 | 不動産の譲渡、地上権の設定、金銭消費貸借 | 1万円未満 |
| 第2号 | 請負に関する契約書（工事、製作など） | 1万円未満 |
| 第17号 | 金銭または有価証券の受取書（領収書） | **5万円未満** |

## 領収書の印紙税額

| 記載金額 | 印紙税額 |
|---|---|
| 5万円未満 | 非課税 |
| 5万円以上 100万円以下 | 200円 |
| 100万円超 200万円以下 | 400円 |
| 200万円超 300万円以下 | 600円 |
| 300万円超 500万円以下 | 1,000円 |
| 500万円超 1,000万円以下 | 2,000円 |
| 1,000万円超 2,000万円以下 | 4,000円 |
| 2,000万円超 3,000万円以下 | 6,000円 |

## 消費税の書き方で印紙代が変わる

**これが実務で最も効く工夫です。** 消費税額が明確に区分されている場合、印紙税は **税抜金額** で判定できます。

税抜45,000円・消費税4,500円（合計49,500円）の取引を例にすると、

| 領収書の書き方 | 判定額 | 印紙 |
|---|---|---|
| 「49,500円」とだけ書く | 49,500円 | 不要（5万円未満） |
| 税抜52,000円・税込57,200円を「57,200円」とだけ書く | 57,200円 | **200円が必要** |
| 同じ取引を「本体52,000円 消費税5,200円 合計57,200円」と書く | 52,000円 | **200円が必要** |
| 税抜48,000円・税込52,800円を「52,800円」とだけ書く | 52,800円 | **200円が必要** |
| 同じ取引を「本体48,000円 消費税4,800円 合計52,800円」と書く | 48,000円 | **不要** |

**税抜では5万円未満なのに、税込にすると5万円を超えてしまう取引**（税抜45,455円〜49,999円）では、書き方を変えるだけで200円の差が出ます。

なお、非課税になるのは **5万円未満** です。税抜がちょうど50,000円の場合は「5万円以上」にあたるため、区分記載をしても200円の印紙が必要になります。

なお、税抜価格と税込価格の両方が書かれている場合も、消費税額が計算できるため税抜で判定できます。

## 印紙が不要になるケース

- **クレジットカード払い**: 金銭の受け取りにあたらないため非課税。ただし領収書に「クレジットカード利用」と明記が必要です。書き忘れると課税されます
- **電子データで発行した領収書・契約書**: PDFをメールで送る、電子契約サービスを使うといった場合、**文書の作成にあたらないため印紙は不要** です。これが電子契約の大きな利点になっています
- **営業に関しないもの**: 個人間の取引や、公益法人が作成する受取書など
- **金額の記載がない領収書**: 「代金として」など具体的な金額を書かない場合は200円（第17号の2）

紙で印刷して押印すると課税されるため、**同じ内容でも電子のままなら非課税** という点が重要です。

## 貼り忘れたときの罰則

印紙を貼らなかった場合、本来の印紙税額の **3倍** が過怠税として課されます（自主的に申し出た場合は1.1倍）。

消印を押し忘れた場合も、印紙の額面と同額の過怠税がかかります。印紙と文書にまたがるように押印してください。

なお、**印紙を貼っていなくても契約自体は有効** です。印紙税は文書作成に対する税であり、契約の成立要件ではありません。

## 貼りすぎたときは還付できる

必要のない文書に印紙を貼ってしまった、金額を間違えて多く貼ってしまった場合は、税務署で還付を受けられます。

- **印紙を貼った文書そのもの** を持参する（切り取らないこと）
- 「印紙税過誤納確認申請書」を提出する
- 印鑑と本人確認書類が必要

ただし、**未使用の収入印紙は現金と交換できません**。郵便局で手数料（1枚5円）を払って別の額面の印紙に交換することはできます。

> 印紙税の判断は文書の内容によって細かく分かれます。金額が大きい場合や判断に迷う場合は、税務署または税理士にご確認ください。
`,

  faq: [
    {
      q: "領収書に収入印紙が必要なのはいくらからですか？",
      a: "記載金額5万円以上からです。5万円ちょうどでも200円の印紙が必要になります。ただし消費税額を分けて記載していれば税抜金額で判定できるため、税抜が5万円未満であれば不要です。",
    },
    {
      q: "消費税額を分けて書くと印紙代が変わるのはどんなときですか？",
      a: "税抜では5万円未満なのに、税込にすると5万円以上になる取引のときです。たとえば税抜48,000円・税込52,800円の場合、合計額だけ書くと200円必要ですが、消費税額を明記すれば税抜48,000円で判定できるため不要になります。",
    },
    {
      q: "電子契約やPDFの領収書に印紙は必要ですか？",
      a: "不要です。印紙税は紙の文書の作成に対してかかるもので、電子データのやり取りは課税対象になりません。ただし印刷して押印し、紙の文書として交付すると課税されます。",
    },
    {
      q: "クレジットカード払いの領収書に印紙は必要ですか？",
      a: "不要です。ただし領収書に「クレジットカード利用」と明記する必要があります。記載がないと現金の受取書とみなされ、課税されます。",
    },
    {
      q: "印紙を貼り忘れるとどうなりますか？",
      a: "本来の印紙税額の3倍の過怠税が課されます（自主的に申し出た場合は1.1倍）。ただし印紙を貼っていなくても契約自体は有効です。",
    },
  ],
};
